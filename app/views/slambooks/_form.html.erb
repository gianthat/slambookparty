<%= form_for(@slambook) do |f| %>
  <% if @slambook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slambook.errors.count, "error") %> prohibited this slambook from being saved:</h2>

      <ul>
      <% @slambook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
