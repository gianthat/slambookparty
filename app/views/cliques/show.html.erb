<h1><%= @clique.name %></h1>

<h3>Members:</h3>

<% @clique.users.each do |user| %>
	<p><%= user.name_with_initial %></p>
<% end %>



<%= link_to 'Edit', edit_clique_path(@clique) %> |
<%= link_to 'Back', cliques_path %>

<p><%= link_to 'Go To Slambook', slambook_path(@clique.slambook) %></p>