<div class="container">

<h1><%= "#{@user.first_name}'s Homeroom" %></h1>

<p>Hey <%=@user.first_name%>! Welcome to Slambook. Each clique has their own slambook. Start out by joining a clique or starting your own!</p>

<h3>Your Cliques</h3>
	<% if @user.cliques.any? %>
		<% @user.cliques.each do |clique| %>
			<p><%= clique.name %></p>
		<% end %>
	<% else %>
		<p>You're not in any cliques yet.</p>
	<% end %>

<%= link_to "Start a clique", new_clique_path, class: "btn btn-info btn-lg" %>
<%= link_to "Join a clique", cliques_path , class: "btn btn-info btn-lg" %>

</div>